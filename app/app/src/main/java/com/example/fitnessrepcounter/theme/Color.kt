package com.example.fitnessrepcounter.theme

import androidx.compose.ui.graphics.Color

// ── Primary palette ── dark, bold, fitness-forward ──
val Charcoal = Color(0xFF0D0D0F)        // deepest background
val DarkSurface = Color(0xFF161619)      // card / surface background
val MidGray = Color(0xFF2A2A2E)          // elevated surfaces
val SubtleGray = Color(0xFF3E3E44)       // borders, dividers

// ── Accent: electric lime/green — energy, motion, go ──
val ElectricLime = Color(0xFFB8FF00)     // primary accent
val ElectricLimeDim = Color(0xFF8BCC00)  // pressed / secondary accent

// ── Status colors ──
val ValidGreen = Color(0xFF00E676)       // rep validated
val RejectRed = Color(0xFFFF5252)        // rep rejected
val WarningAmber = Color(0xFFFFAB00)     // calibration warning
val InfoBlue = Color(0xFF448AFF)         // informational

// ── Text ──
val TextPrimary = Color(0xFFF5F5F5)      // high-emphasis text
val TextSecondary = Color(0xFFB0B0B4)    // medium-emphasis text
val TextDisabled = Color(0xFF6B6B70)     // low-emphasis text

// ── Overlay ──
val OverlayBlack = Color(0xCC000000)     // soft lock overlay (80% black)
val SkeletonLime = Color(0xAAB8FF00)     // pose skeleton overlay color
